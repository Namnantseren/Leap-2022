// function reverse_a_number(n)
// {
// 	n = n + "";
// 	return n.split("").reverse().join("");
// }
// console.log(Number(reverse_a_number(45361)));

while (true) {
    hoho = prompt("Who the fuck are you?")
    let firstChar = number.charAt(0);
    number = Number(number);
    string = String(stirng);
    if (Number.isInteger(number) && (firstChar = "9"|| firstChar == "8")) {
    }else(String.isInteger(string) && (firstChar = "a" || firstChar == "s"))
    {
        output = prompt("I said who the fuck are u!?!?!?")
        break;
    }
}